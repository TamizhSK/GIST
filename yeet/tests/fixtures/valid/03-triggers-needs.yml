on:
  push:
    branches: [main]
    paths: ["src/**"]
  pull_request:
    types: [opened, synchronize]
  schedule:
    - cron: "0 2 * * *"
jobs:
  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - run: echo test
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo build
